When re.split() contains capturing groups, Python includes each captured group's value as a separate element in the output list. When the optional group (\.\d+)* does not match (e.g. for "1. " without a sub-clause), Python inserts None for that group. So the list looked like:
